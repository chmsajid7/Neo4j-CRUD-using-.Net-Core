using Neo4j.Driver;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace Neo4j.Repository
{
    public interface INeo4jRepository
    {
        //IDriver GetContext();
        //Task<List<string>> ReadAllRecordsNeo4jTest();
        //Task<bool> UpdateUser(UserProfile obj);
        //Task<bool> UpdateUserProfilePicture(string loggedInUserId, string userProflePicture);
        //Task<bool> CreateConnection(ConnectionRequest existedConnectionRequest);
        //Task<bool> RemoveConnection(string loggedInUserId, string userId);
        //public Task<List<ConnectionsViewModel>> GetAllConnections(string userId, int page, int pageSize);
        //public Task<List<string>> GetAllConnectionsIds(string userId);
        //public Task<ConnectionsViewModel> GetVisitedMutualConnections(string userId, string visitedUserId);
        //public Task<List<ConnectionsViewModel>> GetMutualConnectionsForSearchResults(string userId, List<string> searchedUserList);
    }
}
